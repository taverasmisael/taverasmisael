{
  "name": "taverasmisael",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:taverasmisael/taverasmisael.git",
  "author": "Misael Taveras <macrot29@gmail.com>",
  "license": "MIT",
  "engines": {
    "node": ">=10.x.x"
  },
  "scripts": {
    "clean": "lerna clean",
    "deploy": "lerna run deploy",
    "empty-cache": "lerna run empty-cache",
    "init": "cd studio && sanity init && lerna run graphql-deploy",
    "format": "lerna run format",
    "graphql-deploy": "lerna run graphql-deploy",
    "lint": "lerna run lint",
    "postinstall": "lerna bootstrap",
    "sanity-deploy": "lerna run sanity-deploy",
    "start:clean": "lerna run empty-cache && lerna run start --parallel",
    "start": "lerna run start --parallel",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "lerna": "^3.14.1",
    "rimraf": "^2.6.3"
  }
}
