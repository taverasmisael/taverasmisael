import React from 'react'
import GeneralLayout from '../layout/general'
import { Typography } from '@material-ui/core'

export default () => (
  <GeneralLayout>
    <Typography variant="h1">Site Title</Typography>
  </GeneralLayout>
)
